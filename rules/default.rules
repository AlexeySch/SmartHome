var String prev_Power_Status = "OB"

rule "SMS on Power state chage"
	when
		Item Power_Status changed or
		System started 
	then

		if(Power_Status=="OL" && prev_Power_Status == "OB") {
		//	logInfo("APP", "Power Up")
		//	executeCommandLine("/etc/openhab2/scripts/power-up.sh")
		}

		if(Power_Status=="OB" && prev_Power_Status == "OL") {
		//	logInfo("APP", "Power Down)
		//	executeCommandLine("/etc/openhab2/scripts/power-down.sh")
		}
		
		
		prev_Power_Status = Power_Status
end

rule "Test Rule"
	when
		Time cron "0 0 0 0 0 ?" 
	then
		var String message = ""
		var String phone = "+79065480824"
			message = "System/Power is up";

			var String cmd = 'echo To > /tmp/testsms.msg'
			logInfo("SMS", cmd)
			executeCommandLine(cmd)
end
